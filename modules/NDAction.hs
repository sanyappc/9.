{-
 - Module : NDAction.hs
 - Description : Модуль, для описания типа Action, для манипулирования со стеком.
 -		Сами типы описаны в NDType (цитаты хранят NDActionPos).
 - Stability : experimental
-}

module NDAction (NDAction(..), NDActionPos(..)) where

import NDType

